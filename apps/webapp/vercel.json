{
  "ignoreCommand": "bash -c 'cd \"$(git rev-parse --show-toplevel)\" && git diff HEAD^ HEAD --quiet -- apps/ services/ scripts/ .github/ turbo.json pnpm-lock.yaml package.json || exit 1; exit 0'"
}
