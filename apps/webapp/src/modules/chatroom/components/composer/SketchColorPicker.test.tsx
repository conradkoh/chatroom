import { render, screen } from '@testing-library/react'; import userEvent from '@testing-library/user-event'; import { describe, expect, it, vi } from 'vitest'; import { SketchColorPicker } from './SketchColorPicker';
describe('SketchColorPicker', () => { it('renders eight colors and selects one', async () => { const onChange = vi.fn(); render(<SketchColorPicker value="#171717" onChange={onChange} />); expect(screen.getAllByRole('button')).toHaveLength(8); await userEvent.click(screen.getByRole('button', { name: 'Brush color #ef4444' })); expect(onChange).toHaveBeenCalledWith('#ef4444'); }); });
