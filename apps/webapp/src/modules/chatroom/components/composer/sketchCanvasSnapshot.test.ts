import { describe, expect, it, beforeAll } from 'vitest'; import { imageDataPixelsEqual, sketchCanvasHasInk } from './sketchCanvasSnapshot';
beforeAll(() => { if (!globalThis.ImageData) { class TestImageData { data: Uint8ClampedArray; width: number; height: number; constructor(width: number, height: number) { this.width = width; this.height = height; this.data = new Uint8ClampedArray(width * height * 4); } } Object.assign(globalThis, { ImageData: TestImageData }); } });
describe('sketchCanvasSnapshot',()=>{it('detects ink and compares pixels',()=>{const a=new ImageData(1,1);a.data[0]=255;a.data[1]=255;a.data[2]=255;a.data[3]=255;expect(sketchCanvasHasInk(a)).toBe(false);a.data[0]=1;expect(sketchCanvasHasInk(a)).toBe(true);expect(imageDataPixelsEqual(a,a)).toBe(true);});});
