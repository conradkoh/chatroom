'use client';
import { useIsDesktop } from '@/hooks/useIsDesktop';
export function SketchZoomControls({ zoom, onZoomChange }: { zoom: number; onZoomChange: (zoom: number) => void }) { const desktop = useIsDesktop(); const percent = Math.round(zoom * 100); if (!desktop) return <span className="text-[11px] text-chatroom-text-muted" aria-live="polite">{percent}% — pinch to zoom</span>; return <div className="flex items-center gap-2"><label htmlFor="sketch-zoom" className="text-[11px] text-chatroom-text-muted">Zoom</label><input id="sketch-zoom" type="range" min={50} max={200} step={10} value={percent} aria-valuetext={`${percent}%`} onChange={(e) => onZoomChange(Number(e.target.value) / 100)} className="h-1 w-24 cursor-pointer accent-chatroom-accent" /><span className="text-[11px] text-chatroom-text-muted w-8">{percent}%</span></div>; }
