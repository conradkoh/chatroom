'use client';
import type { SketchFloatingSelection, SketchRect, ResizeHandle } from './sketchSelectionTypes';
export function SketchSelectionOverlay({zoom,marquee,selection,onHandlePointerDown}:{zoom:number;marquee:SketchRect|null;selection:SketchFloatingSelection|null;onHandlePointerDown:(h:ResizeHandle,e:React.PointerEvent)=>void}){const r=marquee??selection?.bounds; if(!r)return null; return <div className="absolute inset-0 pointer-events-none"><div className="absolute border border-dashed border-chatroom-accent" style={{left:r.x*zoom,top:r.y*zoom,width:r.width*zoom,height:r.height*zoom}}>{selection&&(['nw','n','ne','e','se','s','sw','w'] as ResizeHandle[]).map(h=><button key={h} aria-label={`Resize ${h}`} className="pointer-events-auto absolute h-2 w-2 bg-chatroom-accent" style={{left:h.includes('w')?-4:h.includes('e')?`calc(100% - 4px)`:'calc(50% - 4px)',top:h.includes('n')?-4:h.includes('s')?'calc(100% - 4px)':'calc(50% - 4px)'}} onPointerDown={e=>{e.stopPropagation();e.preventDefault();onHandlePointerDown(h,e)}}/>)}</div></div>;}
