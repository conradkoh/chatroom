import { SKETCH_CANVAS_COLORS } from './sketchConstants'; import { toBackingRect } from './sketchCanvasCoords'; import type { SketchRect } from './sketchSelectionTypes';
export function scaleImageData(source:ImageData,w:number,h:number){const src=document.createElement('canvas');src.width=source.width;src.height=source.height;src.getContext('2d')!.putImageData(source,0,0);const dst=document.createElement('canvas');dst.width=Math.max(1,Math.round(w));dst.height=Math.max(1,Math.round(h));const c=dst.getContext('2d')!;c.imageSmoothingEnabled=false;c.drawImage(src,0,0,dst.width,dst.height);return c.getImageData(0,0,dst.width,dst.height);}
export async function imageDataToPngBlob(data:ImageData){const c=document.createElement('canvas');c.width=data.width;c.height=data.height;c.getContext('2d')!.putImageData(data,0,0);return new Promise<Blob|null>(r=>c.toBlob(r,'image/png'));}
export function fillRectWithBackground(ctx:CanvasRenderingContext2D,r:SketchRect,background=SKETCH_CANVAS_COLORS.background){ctx.fillStyle=background;ctx.fillRect(r.x,r.y,r.width,r.height);}
export function drawImageDataAt(ctx:CanvasRenderingContext2D,data:ImageData,r:SketchRect){const c=document.createElement('canvas');c.width=data.width;c.height=data.height;c.getContext('2d')!.putImageData(data,0,0);ctx.drawImage(c,r.x,r.y,r.width,r.height);}
export function liftPixels(ctx:CanvasRenderingContext2D,canvas:HTMLCanvasElement,r:SketchRect,dpr:number){const b=toBackingRect(r,dpr);if(b.width<1||b.height<1)return null;const data=ctx.getImageData(b.x,b.y,b.width,b.height);fillRectWithBackground(ctx,r);return data;}
