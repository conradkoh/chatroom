# Convex → Daemon Incremental Sync

The canonical guide lives next to the library:

**[packages/cli/src/infrastructure/incremental-sync/README.md](../../../packages/cli/src/infrastructure/incremental-sync/README.md)**

That README covers transport, orchestration (`runDualChannelFeedLive`), `WorkingSnapshot`, backend conventions, and the task-monitor reference implementation.
