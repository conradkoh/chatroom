import { httpRouter } from 'convex/server';

const http = httpRouter();

// HTTP routes can be added here as needed.
// Telegram integration is output-only — no inbound webhook routes required.

export default http;
