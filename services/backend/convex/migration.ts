/**
 * Database Migrations (DEPRECATED)
 *
 * @deprecated All migrations have been moved to `migrations.ts` using the
 * @convex-dev/migrations framework. This file is kept empty to maintain
 * compatibility with the auto-generated Convex API types.
 *
 * To run migrations: `npx convex run migrations:runAll`
 * To add new migrations: Use `migrations.define()` in `migrations.ts`
 *
 * Previously defined migrations (all ported to migrations.ts):
 * - migrateAvailableModelsToPerHarness
 * - stripParticipantStaleFields
 * - deleteOldFormatAgentPreferences
 * - deleteLegacyMessageQueueDocuments
 * - migrateQueuedTasks
 * - migrateTeamRoleKeyAddTeamId
 * - migrateStopReasonToActorPrefixed
 * - migrateEventReasonsToActorPrefixed
 * - deduplicateTeamAgentConfigs
 * - purgeWorkspaceCommitDetails
 */

// File intentionally empty — all migrations now live in migrations.ts
