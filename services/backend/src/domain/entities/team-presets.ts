export {
  DEFAULT_TEAM_PRESET_ID,
  TEAM_PRESETS,
  type TeamPreset,
  type TeamStructure,
  getTeamPreset,
  getTeamStructure,
  listTeamPresetIds,
} from '@workspace/shared/domain/team-presets';

export type { TeamPresetId } from '@workspace/shared/domain/team-presets';
