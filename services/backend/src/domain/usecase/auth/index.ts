/**
 * Auth domain — re-exports from extensions.
 *
 * Upstream auth lives in modules/auth/ (do not modify).
 * Custom auth extensions live in ./extensions/.
 */

export * from './extensions/index.js';
